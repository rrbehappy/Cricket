capsule {
  id (playground.cricor)
  version (1.0.0)
  format(3)
  runtime-version (8)
  targets {
    target (bixby-mobile-en-US)
    // target (bixby-tv-en-US)
    // target (bixby-watch-en-US)
    // target (bixby-fridge-en-US)
  }
  default-action (FindPlayer)
  capsule-imports {
    import (viv.core) {
      as (core)
    }
    import (viv.money) { as (money) version (2.22.53) }
    //import (viv.navigation) { as (navigation) version (1.10.10) }
    //import (viv.time) { as (time) version-from (navigation) }
    //import (viv.geo) { as (geo) version-from (navigation) }
  }
  store-countries {
    all
  }
  store-sections {
    // section (ExampleSection)
  }
  permissions {
   // Remove comment to access user's current location or profile
   // user-profile-access
  }
}